import { UserWaitlistForm } from "../components/user-waitlist-form";
import { Layout } from "../components/Layout";

export const Waitlist = () => {
  return (
    <Layout>
      <UserWaitlistForm />
    </Layout>
  );
};
